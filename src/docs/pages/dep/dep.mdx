# 部分需要替换的依赖

*  linux-raw-sys(https://github.com/zhaixiaojuan/linux-raw-sys.git)

# 需要手动patch的依赖

## libsanitizer
*   需要将`sanitizer_linux.cpp`中的所有`SC_ADDRERR_WR`与`SC_ADDRERR_RD` 替换成 `ADRERR_WR`与`ADRERR_RD` 这个是新旧世界的差异导致的
